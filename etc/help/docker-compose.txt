# Start compose file with name of docker-compose.yml
docker-compose up

# Start compose file with different name than docker-compose.yml
docker-compose -f full-smart-platform.yml up
docker-compose -f smart-platform-default.yml up

# Stop compose file with different name than docker-compose.yml
docker-compose -f full-smart-platform.yml down